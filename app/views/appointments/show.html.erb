<h1>Appointment</h1>
<%= link_to @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M")%> 
<%= link_to @appointment.doctor.name, doctor_path %> 
<%= link_to @appointment.patient.name, patient_path %>